# backend/.env
FLASK_ENV=development
SECRET_KEY='a_very_secret_random_string_here' # Use your actual secret key

# Google OAuth (keep these as before)
GOOGLE_CLIENT_ID='206150942567-lqb9kraptc3lg932pkj9l5i102eihjhe.apps.googleusercontent.com'
GOOGLE_DISCOVERY_URL="https://accounts.google.com/.well-known/openid-configuration"

# --- Cloud SQL Connection Details ---
# Use the Cloud SQL Python Connector approach
DB_USER='admin'
DB_PASSWORD='admin'
DB_NAME='postgres'
DB_CONNECTION_NAME='learningproj-365113:us-central1:react-db' # Your instance connection name

# Frontend URL (keep as before)
FRONTEND_URL='http://localhost:3000'

# Remove or comment out any old DATABASE_URI variable if it exists
# DATABASE_URI='...' # Not directly used by the connector method below